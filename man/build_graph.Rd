% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dijkstra.R
\name{build_graph}
\alias{build_graph}
\title{Build a Graph in CSR-like Format}
\usage{
build_graph(n, u, v, w, directed = FALSE)
}
\arguments{
\item{n}{an integer specifying the number of nodes (vertices) in the graph.}

\item{u}{an integer vector of the starting nodes for each edge (1-indexed).}

\item{v}{an integer vector of the ending nodes for each edge (1-indexed).}

\item{w}{a numeric vector of edge weights (costs).}

\item{directed}{a logical value indicating whether the graph is directed. Defaults to \code{FALSE} (undirected).}
}
\value{
\code{build_graph()} returns a list containing the internal graph representation (\code{n}, \code{head}, \code{to}, \code{weight}, \code{nxt}, and \code{alloc_size}).
}
\description{
Constructs a graph using a 1D-array-based Compressed Sparse Row (CSR) format.
This structure is highly optimized for fast traversal in algorithms like Dijkstra's.
Separating the graph construction from the path-finding algorithm avoids redundant processing when running algorithms multiple times on the same graph.
}
\examples{
n <- 4
u <- c(1, 1, 2, 3)
v <- c(2, 3, 4, 4)
w <- c(2, 5, 1, 3)
graph <- build_graph(n, u, v, w)
}
